{
  "actions": {
    "build": {
      "enabled": true,
      "script": "pnpm -F @groton/knowledgebase.router run build"
    },
    "run": {
      "enabled": true,
      "script": "pnpm -F @groton/knowledgebase.router run build\npnpm -w run deploy:deploy"
    }
  },
  "openLogOnRun": "start"
}
